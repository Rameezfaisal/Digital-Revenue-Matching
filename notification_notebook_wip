try:
    SMTP_SERVER = dbutils.widgets.get("smtp_server")
except Exception:
    print("Warning: 'smtp_server' parameter not provided. Defaulting to 'localhost'.")
    SMTP_SERVER = 'localhost'  # Update this to your actual default




# Initialize the widget with a default value (e.g., 'localhost' or 'smtp.office365.com')
dbutils.widgets.text("smtp_server", "localhost") 

# Now this will never throw an error, even if run manually
SMTP_SERVER = dbutils.widgets.get("smtp_server")






# Configuration Settings
dbutils.widgets.text("smtp_server", "smtp.office365.com") # Or your chosen default
SMTP_SERVER = dbutils.widgets.get("smtp_server")
SMTP_PORT = 25  
SENDER_EMAIL = "noreply-databricks@wescodist.com"

# Update your recipient list here
RECIPIENTS = [
    "rameez.faisal@wescodist.com"
    # "BI_Wesco_Integration_Support_L2@wescodist.com",
    # "srichard@wescodist.com",
    # "vgolla@wescodist.com",
    # "srilatha.mahankali@wescodist.com"
]

SUBJECT = "Digital Dashboard and Source records comparision Daily"

