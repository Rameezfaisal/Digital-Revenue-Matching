SELECT 
    datasource_num_id, 
    branch_id, 
    date_invoice, 
    invoice_no, 
    sales_order_no,
    ent_type,
    LENGTH(sales_order_no) as actual_length
FROM brz_teradata.udm_order_fact
WHERE sales_order_no LIKE '%24FB24GP%';
