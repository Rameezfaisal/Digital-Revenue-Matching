SELECT 
    EXTRACT(YEAR FROM date_invoice) AS yr, 
    EXTRACT(MONTH FROM date_invoice) AS mth, 
    COUNT(*) AS row_count
FROM brz_teradata.udm_sales_fact -- (Use dss_marts.udm_sales_fact in Teradata)
WHERE to_date(date_invoice) >= '2022-01-01'
GROUP BY 1, 2 
ORDER BY 1, 2;
